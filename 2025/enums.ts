export enum DSSExtension {
    XML = "xml",
    JSON = "json"
}

export enum DSSFile {
    CareerSavegame = "careerSavegame",
    Vehicles = "vehicles",
    Economy = "economy"
}

export enum WebAPIJSONAction {
    Ping = "ping",
    RestartServer = "restartServer",
    StartServer = "startServer",
    StopServer = "stopServer"
}